# Results Storage

Raw benchmark output stays local and is ignored by Git. Check in only curated result records under `leaderboards/data/` or a future `benchmarks/runs/` directory.

Curated records must omit raw model answers, server logs, credentials, local absolute paths, and temporary stack traces. They should retain scores, throughput, memory, provenance, suite version, input hashes, and links to local evidence where necessary.
